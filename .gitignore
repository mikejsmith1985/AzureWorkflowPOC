# Build outputs
bin/
obj/

# User-specific files
*.user
*.suo
.vs/

# Rider
.idea/

# Secrets — NEVER commit these
appsettings.Development.json
appsettings.*.json
!appsettings.json

# NuGet packages
*.nupkg
.nuget/

# Coverage
TestResults/
coverage/

# Local SQLite database — runtime data, never commit
*.db
*.db-wal
*.db-shm
*.db.bak-*
*.db-wal.bak-*
*.db-shm.bak-*

# Machine-specific SDK resolution (hardcoded user-local dotnet path)
global.json

# Local release output — zip archives and unpacked publish directories
publish/

# Local tooling — not part of the project
.forge/
.github/
CLAUDE.md

# Generated ADO bootstrap manifest (runtime output, not source)
src/DBAIAzure.Web/specs/

# Deploy-time secret env files — values come from the Forge Vault, NEVER committed (Article IX)
deploy/aca/team.env
**/*.env
!**/*.env.example

# ADO preflight runtime manifest (generated)
specs/.ado-bootstrap-manifest.json
