@page "/new-ticket"
@inject PipelineOrchestrator Orchestrator
@inject NavigationManager Nav

<h1 class="text-xl font-semibold text-fg mb-6">Submit a Ticket</h1>

<div class="bg-surface border border-line rounded-lg p-6 max-w-2xl">
    <EditForm Model="@_model" OnValidSubmit="HandleSubmitAsync">
        <DataAnnotationsValidator />

        <div class="mb-4">
            <label class="block text-sm text-muted mb-1">Ticket ID</label>
            <InputText @bind-Value="_model.TicketId"
                       class="w-full bg-raised border border-line rounded px-3 py-2 text-fg
                              focus:outline-none focus:border-accent text-sm" />
        </div>

        <div class="mb-4">
            <label class="block text-sm text-muted mb-1">Title</label>
            <InputText @bind-Value="_model.Title"
                       class="w-full bg-raised border border-line rounded px-3 py-2 text-fg
                              focus:outline-none focus:border-accent text-sm"
                       placeholder="Concise active-voice title" />
        </div>

        <div class="mb-6">
            <label class="block text-sm text-muted mb-1">Description</label>
            <InputTextArea @bind-Value="_model.Description" rows="6"
                           class="w-full bg-raised border border-line rounded px-3 py-2 text-fg
                                  focus:outline-none focus:border-accent text-sm resize-none"
                           placeholder="Include acceptance criteria and technical context…" />
        </div>

        <div class="flex gap-3">
            <button type="submit" disabled="@_isSubmitting"
                    class="px-5 py-2 bg-accent hover:opacity-90 disabled:opacity-50 text-onaccent
                           text-sm rounded transition-colors">
                @(_isSubmitting ? "Starting…" : "Start Pipeline")
            </button>
            <a href="/" class="px-5 py-2 text-sm text-muted hover:text-fg transition-colors">
                Cancel
            </a>
        </div>
    </EditForm>
</div>

@code {
    private readonly TicketFormModel _model = new()
    {
        TicketId = $"INC{Random.Shared.Next(1000000, 9999999)}",
    };

    private bool _isSubmitting;

    private async Task HandleSubmitAsync()
    {
        _isSubmitting = true;
        StateHasChanged();

        var ticket = new TicketState
        {
            TicketId = _model.TicketId.Trim(),
            Title = _model.Title.Trim(),
            Description = _model.Description.Trim(),
        };

        var runId = Orchestrator.StartRun(ticket);
        await Task.Delay(100); // let the background task start before navigating
        Nav.NavigateTo($"/run/{runId}");
    }

    private sealed class TicketFormModel
    {
        public string TicketId { get; set; } = string.Empty;
        public string Title { get; set; } = string.Empty;
        public string Description { get; set; } = string.Empty;
    }
}
